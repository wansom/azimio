import 'package:azimio/screens/home/home.dart';
import 'package:flutter/material.dart';

class AuthWarapper extends StatelessWidget {
  const AuthWarapper({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Home();
  }
}
